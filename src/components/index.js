/**
 * 收集components下的各个组件
 * 引入components下的组件的时候只需要一行语句解构组件
 * import { Sidebar, Navbar, AppMain } from '@/components/';
 */
export { default as Sidebar } from './Sidebar';
export { default as Navbar } from './Navbar';
export { default as AppMain } from './AppMain';